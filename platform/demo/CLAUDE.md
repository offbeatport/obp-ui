# Demo

Built on `@offbeatport/microsaas-core`. All factory rules - layout,
archetypes, scaffold flow, design, quality gates - live in
[`../../FACTORY.md`](../../FACTORY.md). Read it once.

Per-app context:

- Archetype: tool-first (AI Tagline Generator)
- Niche slot: `src/features/` - the only directory that materially
  varies from siblings.
- Style + Radius: Cobalt + Subtle (factory default)

Per-app overrides (rules in FACTORY.md that are wrong for this app):
<empty>
