# Demo - AI Tagline Generator
#
# Provider keys (OpenRouter) live in the monorepo root's .env.shared.
# Per-app secrets live below.

# === Database ===
DATABASE_URL=./demo.db

# === Auth (better-auth) ===
BETTER_AUTH_SECRET=replace-with-32+-random-chars
BETTER_AUTH_URL=http://localhost:3002
