# ReportFuse

Built on `@offbeatport/microsaas-core`. All factory rules - layout, archetypes,
scaffold flow, design, quality gates - live in
[`../../FACTORY.md`](../../FACTORY.md). Read it once.

Per-app context:

- Archetype: tool-first
- Niche slot: `src/features/` - the only directory that materially
  varies from siblings.
- Style + Radius: Azure + Sharp

Per-app overrides (rules in FACTORY.md that are wrong for this app):
<empty by default - fill in with rationale if you genuinely diverge>
